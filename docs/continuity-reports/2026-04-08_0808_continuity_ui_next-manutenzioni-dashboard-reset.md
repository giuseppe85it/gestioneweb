# Continuity Report - 2026-04-08 08:08

## Stato consegna
- Patch UI completata nel perimetro consentito.

## Obiettivo centrato
- La `Dashboard` di `/next/manutenzioni` non deve piu sembrare una pagina a pannelli pesanti.

## Esito operativo
- header comune compatto confermato;
- `Dashboard` alleggerita e usata come ingresso rapido;
- `Nuova / Modifica` full-width invariata;
- `Dettaglio` mantiene 2 card root vere;
- `Quadro manutenzioni PDF` mantiene filtro sopra + elenco full-width sotto.

## Verifiche da ripetere se si riprende il modulo
- controllare che non ricompaiano macro-card di contesto nella `Dashboard`;
- controllare che `Storico` resti assente;
- controllare che `Tagliando completo` resti solo condizionale;
- controllare che `Apri dettaglio` continui a portare al tab `Dettaglio`.
