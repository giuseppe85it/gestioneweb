# Change Report - Loop `Mezzi` (`2026-03-31 06:52`)

## Obiettivo
Chiudere il modulo `Mezzi` della NEXT sulla route `/next/mezzi` come clone fedele read-only della madre.

## File letti
- `src/App.tsx`
- `src/next/NextMezziPage.tsx`
- `src/next/nextFlottaCloneState.ts`
- `src/next/nextAnagraficheFlottaDomain.ts`
- `src/pages/Mezzi.tsx`
- `docs/audit/TRACKER_NEXT_CLONE_LOOP.md`
- `docs/audit/BACKLOG_mezzi.md`
- `docs/audit/AUDIT_mezzi_LOOP.md`

## Patch applicate
- `NextMezziPage` riallineata alla superficie madre su foto, `LIBRETTO (IA)`, form completo, testi, placeholder, validazioni visibili e lista per categoria.
- Rimossi dal runtime ufficiale `Mezzi` i writer clone-only e i testi clone-only.
- `handleSave()`, `handleDelete()` e `handleAnalyzeLibrettoWithIA()` bloccano il comportamento con messaggio read-only esplicito senza deformare la UI.
- `readNextAnagraficheFlottaSnapshot()` diventa safe di default: patch clone-only solo su richiesta esplicita.

## Verifiche eseguite
- `npx eslint src/next/NextMezziPage.tsx src/next/nextAnagraficheFlottaDomain.ts` -> OK
- `npm run build` -> OK

## Esito operativo
- Il modulo `Mezzi` passa da `FAIL` a `CLOSED` nel tracker.
- L'audit separato del modulo risulta `PASS`.
- Il prossimo modulo non `CLOSED` del loop diventa `Dossier Lista`.
