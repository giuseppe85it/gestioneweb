# STATO ATTUALE DEL PROGETTO

## 1. Situazione generale
- **Audit finale strutturale `Magazzino NEXT` (2026-04-10)**: completato in sola lettura sul dominio reale collegato a `/next/magazzino`, senza toccare il runtime. Fatti verificati nel repo: route e wiring del modulo sono `CHIUSO`; `Cisterne AdBlue` e `CHIUSO` come sottodominio isolato su `@cisterne_adblue`; `Inventario`, `Materiali consegnati`, `Documenti e costi`, compatibilita con Dossier/lettori, compatibilita con writer esterni e parity logica con la madre restano `PARZIALE`. Gap reali confermati: parity PDF legacy non replicata in `NextMagazzinoPage.tsx`, dominio stock ancora multi-writer e non transazionale, rischio concreto di doppio decremento ancora presente in `Acquisti.tsx` e `DettaglioOrdine.tsx`, import inventario IA legacy ancora basato su matching descrittivo. Verdetto finale del dominio: `Magazzino NEXT` resta `PARZIALE`; audit dedicato: `docs/audit/AUDIT_FINALE_MAGAZZINO_NEXT_DOMINIO_2026-04-10.md`.
- **Patch strutturale dominio allargato `Magazzino` NEXT (2026-04-09, tracciabilita completata 2026-04-10)**: eseguita nel perimetro autorizzato senza toccare la madre legacy. `src/next/NextMagazzinoPage.tsx` mantiene ora le shape reali dei dataset multi-writer `@inventario`, `@materialiconsegnati`, `@cisterne_adblue`, preserva i wrapper storage-style (`array/items/value`), tollera unita esterne come `m` e non riscrive piu i record impoverendo i campi cross-modulo. Le nuove consegne scritte dal modulo portano anche `inventarioRefId`, `materialeLabel`, `direzione`, `tipo`, `origine` e `targa/mezzoTarga` quando il destinatario e un mezzo; il delete ripristina lo stock prima via `inventarioRefId` e poi via fallback descrizione+unita+fornitore. La UI aggiunge una quarta sezione `Documenti e costi` in sola lettura con archivio `@documenti_magazzino`, supporto costi/documenti, preview procurement e link dossier/analisi. `nextMaterialiMovimentiDomain.ts` inferisce meglio `MEZZO` / `MAGAZZINO` e raggruppa i destinatari mezzo per targa canonica; `nextStructuralPaths.ts` accetta `?tab=documenti-costi` e risolve i redirect legacy di operativita verso `/next/magazzino?tab=...`. Verifiche gia eseguite nel task runtime: `npx eslint` mirato OK, `npm run build` OK, runtime verificato su `/next/magazzino`, `/next/inventario`, `/next/materiali-consegnati`. Report esecutivi creati e fonti mirrorate riallineate nel follow-up documentale del `2026-04-10`. Stato modulo dopo la patch: `PARZIALE`; chiusura finale demandata ad audit separato.
- **Pre-audit `Magazzino` legacy vs NEXT (2026-04-09, poi superato dalla patch 22:28 e dall'audit finale 2026-04-10)**: eseguito in sola lettura sul dominio reale di `Inventario` e `MaterialiConsegnati`, estendendo la verifica a `DossierMezzo`, `Mezzo360`, `Gestione Operativa`, `Acquisti`, `DettaglioOrdine`, `IADocumenti`, `Manutenzioni` e ai reader NEXT collegati. Ha fissato i fatti di base del dominio: `@inventario` e `@materialiconsegnati` come dataset storage-style multi-writer e non transazionali, `@documenti_magazzino` come collection documentale/costi e non ledger canonico di stock. Il verdetto finale aggiornato del dominio e ora demandato all'audit `docs/audit/AUDIT_FINALE_MAGAZZINO_NEXT_DOMINIO_2026-04-10.md`.
- **Regola architetturale corrente NEXT (2026-04-09 aggiornamento ufficiale)**: la NEXT non e piu da considerare un clone globalmente `read-only`. `src/next/*` e il nuovo perimetro applicativo del gestionale; le scritture reali si aprono solo modulo per modulo, in modo esplicito e controllato, con `cloneWriteBarrier.ts` come punto di enforcement e con documentazione allineata. La madre legacy resta intoccabile.
- **Ingresso unico `Magazzino` nella NEXT con redirect di compatibilita (2026-04-09 PROMPT38 wiring)**: il nuovo modulo unificato `src/next/NextMagazzinoPage.tsx` diventa l'unico ingresso pubblico principale della NEXT per il dominio magazzino. La sidebar centrale e la Home NEXT puntano ora a `/next/magazzino`; i vecchi path `/next/inventario` e `/next/materiali-consegnati` non montano piu i moduli separati come entrypoint principali ma restano attivi solo come redirect `replace` rispettivamente verso `/next/magazzino?tab=inventario` e `/next/magazzino?tab=materiali-consegnati`. Nessuna business logic, nessun writer e nessun barrier sono stati modificati; nessun file vecchio e stato cancellato. Build OK, redirect runtime verificati.
- **Modulo `Magazzino` NEXT nativo con 3 sezioni e dataset storage-style `@cisterne_adblue` (2026-04-09 PROMPT18S)**: creata la nuova route `/next/magazzino` nella shell NEXT con pagina unica `src/next/NextMagazzinoPage.tsx` e CSS dedicato `src/next/next-magazzino.css`. Il modulo riunisce `Inventario`, `Materiali consegnati` e `Cisterne AdBlue` in una UI nativa con switcher interno, form e KPI propri. Le scritture restano clone-safe tramite `cloneWriteBarrier.ts` e sono limitate a `@inventario`, `@materialiconsegnati`, `@cisterne_adblue` e upload immagini su `inventario/*`; `@cisterne_adblue` e trattato come dataset storage-style via `getItemSync/setItemSync`, non come collection Firestore classica. Inclusi i fix richiesti dalla spec su `Materiali consegnati`: blocco stock insufficiente, rollback se fallisce la seconda scrittura e warning prima del ripristino di un item orfano. Build OK. Stato modulo: `PARZIALE`.
- **Cartella stabile `docs/fonti-pronte/` (2026-04-09 PROMPT37B)**: creata la raccolta unica `docs/fonti-pronte/` per avere in un solo posto le fonti piu utili da passare a una nuova chat: `AGENTS.md`, stato progetto, stato NEXT, registro clone, contesto Claude, punti da verificare, protocollo sicurezza, procedura madre->clone, piu audit/change/continuity report chiave e un indice dedicato. Regola permanente fissata in `AGENTS.md` e nei documenti guida: quando si aggiorna uno dei file sorgente specchiati, nello stesso task va aggiornata anche la copia in `docs/fonti-pronte/`. Impatto solo documentale.
- **Euromecc Mega Patch — nuovi SVG diagram, mappa interattiva riepilogo, fix PDF (2026-04-09 PROMPT36)**: il modulo `/next/euromecc` ha ricevuto una patch strutturale. Aggiunta la nuova area `scaricoFornitore` in `euromeccAreas.ts` con 6 subcomponenti (SCF-01-ATT/TUB/QPN/VLV/FIL/COC). Aggiunti `compressore2` e `scaricoFornitore` a `MAP_GENERIC`. Implementati due nuovi diagrammi SVG interattivi: `SalaCompressoriDiagram` (viewBox 760x640, con 2 accumulatori, 2 compressori, 2 essiccatori, 8 hotspot con leader-line) usato per entrambe le aree `compressore` e `compressore2`; `ScaricoFornitoreDiagram` (viewBox 680x700, con silo, tubazioni, attacco fornitore, filtro, coclea, quadro pneumatico, 6 hotspot). Branch condizionali aggiornati nel selettore area. `RiepilogoMappaImpianto` reso interattivo: click su nodo mappa mostra pannello laterale read-only con pending/done/issues per quell'area. Sostituito `html2canvas` con `svgToImageData` (serializzazione XMLSerializer → Blob → Image → Canvas) per mappa e diagram nel PDF. `CaricoTrenoDiagram` non implementato — SERVE FILE EXTRA: corpo SVG CaricoTrenoDiagram. Build OK zero errori TS. Stato modulo: `PARZIALE`.
- **Correzione metriche `Compressore` nel `Quadro manutenzioni PDF` NEXT (2026-04-09 PROMPT35)**: nel tab `Quadro manutenzioni PDF` di `/next/manutenzioni` il ramo `Compressore` non usa piu la stessa metrica del ramo `Mezzo`. In `src/next/NextManutenzioniPage.tsx` il pannello risultati e l'export locale PDF separano ora le metriche in base al filtro attivo: `Mezzo` continua a usare `km`, `Compressore` usa `ore`, mentre `Attrezzature` non forza piu `km/ore` come default ma mostra solo la misura realmente presente nel record (`ore` se disponibili, altrimenti `km`). Nel ramo compressore il quadro mostra quindi `Ore attuali`, `Ore intervento` e `Δ ore` solo se calcolabili, senza etichette `km` residue. Stato modulo: `PARZIALE`.
- **Fix esecutivi cross-modulo `Manutenzioni` NEXT (2026-04-09 PROMPT34)**: applicato il primo blocco di correzioni emerso dall'audit `PROMPT32` senza toccare la madre. In `src/next/domain/nextManutenzioniDomain.ts` il writer materiali non esegue piu due `setItemSync()` consecutivi su `@materialiconsegnati`: il flusso salva ora un solo record unificato per ogni materiale manutenzione, preservando nello stesso dataset sia i campi utili ai reader materiali/movimenti sia quelli usati dal Dossier mezzo-centrico. In parallelo `nextManutenzioniGommeDomain.ts` privilegia davvero `gommePerAsse`, `gommeInterventoTipo` e `gommeStraordinario` prima del parsing legacy; `NextGommeEconomiaSection.tsx` espone in modo distinto stato ordinario per asse ed eventi straordinari con motivo; `nextDossierMezzoDomain.ts` + `NextDossierMezzoPage.tsx` mostrano ora nel Dossier principale il riepilogo gomme per asse e gli straordinari; `nextOperativitaGlobaleDomain.ts` costruisce descrizioni e flag piu coerenti per gli interventi gomme strutturati. Compatibilita legacy mantenuta tramite fallback prudente sulla descrizione. Stato modulo: `PARZIALE`.
- **Audit profondo cross-modulo `Manutenzioni` NEXT (2026-04-09 PROMPT32)**: eseguito in sola lettura sul codice reale di route, reader, writer e moduli collegati. `/next/manutenzioni` resta un runtime NEXT nativo e il modulo continua a leggere/salvare su fonti reali coerenti (`@manutenzioni`, `@mezzi_aziendali`, layer rifornimenti, convergenza gomme), ma l'audit NON consente chiusure: `Manutenzioni NEXT`, `Dettaglio`, `Quadro manutenzioni PDF`, `Collegamento con Dossier`, `Collegamento con App Autisti` e `Boundary NEXT vs Madre` risultano tutti `PARZIALE`. Fragilita principale emersa: il writer materiali di `src/next/domain/nextManutenzioniDomain.ts` esegue due scritture consecutive sulla stessa chiave `@materialiconsegnati`, quindi il primo payload viene sovrascritto dal secondo; inoltre il quadro usa i campi strutturati gomme in modo piu ricco del Dossier, che oggi continua a ridurre le manutenzioni a una vista legacy e nel ramo `Gomme` dipende ancora in larga parte dal parsing della descrizione testuale. Audit dedicato: `docs/audit/AUDIT_MANUTENZIONI_NEXT_CROSSMODULO_PROMPT32_2026-04-09.md`. Stato modulo: `PARZIALE`.
- **Foto reale mezzo nel PDF `Quadro manutenzioni` + pulizia microcopy `Manutenzioni` NEXT (2026-04-09 PROMPT31B)**: l'export PDF del tab `Quadro manutenzioni PDF` non passa piu dal solo export tabellare generico del motore condiviso. Nel modulo `/next/manutenzioni` il ramo locale `exportPdfForItems(...)` costruisce ora un PDF dedicato con jsPDF/autotable e, quando l'export riguarda una sola targa, stampa in testata la **foto reale del mezzo** presa da `fotoUrl` del preview mezzo associato alla targa; se la foto reale manca, il PDF usa un fallback pulito senza sostituirla con tavole tecniche `public/gomme/*`. Nello stesso task sono stati rimossi testi fissi ridondanti da `Dashboard`, `Nuova / Modifica`, `Quadro manutenzioni PDF` e `Dettaglio`, spostando le spiegazioni essenziali su tooltip nativi (`title`) dei controlli rilevanti. Stato modulo: `PARZIALE`.
- **Ripristino viste tecniche `Sinistra / Destra` + ricerca rapida nel quadro `Manutenzioni` NEXT (2026-04-09 PROMPT30)**: il tab `Dettaglio` di `/next/manutenzioni` riaggancia nel ramo embedded il mapping tecnico gia esistente verso `public/gomme/*` tramite `resolveNextManutenzioneTechnicalView(...)`, usando la categoria reale del mezzo e senza reintrodurre `Calibra`, marker o overlay. Nel runtime del dettaglio restano solo le viste `Sinistra` e `Destra`; `Fronte` e `Retro` non compaiono piu nel ramo embedded. Il tab `Quadro manutenzioni PDF` espone ora anche una ricerca rapida visibile che filtra i risultati per `targa` e `autista`, separata dalla ricerca generale di pagina. Stato modulo: `PARZIALE`.
- **Nuova UI tab Riepilogo Euromecc + export PDF (2026-04-09 PROMPT29)**: il tab `Riepilogo` di `/next/euromecc` e stato completamente sostituito. Il vecchio JSX con textarea e `window.print()` e rimosso. La nuova UI espone: barra filtro periodo con bottone `Esporta PDF`, `KpiGrid` esistente, mappa impianto SVG read-only con legenda colori, card per area ordinate per urgenza (prima le urgenze, poi pending, poi solo-done), layout a due colonne per i punti di carico (`carico1`, `carico2`, `caricoRail`) con overlay frecce SVG colorate sui componenti attivi. L'export PDF usa jsPDF + jspdf-autotable con import lazy e html2canvas per la mappa; il salvataggio archivio su Firestore non e implementato (fase 2, escluso esplicitamente). `html2canvas ^1.4.1` aggiunto a `package.json` come dipendenza esplicita. `MapSvg`, `SiloDiagram`, `CaricoDiagram`, altri tab, Firestore non toccati. Build OK. Stato modulo: `PARZIALE`.
- **Rimozione completa `Calibra` dal `Dettaglio` di `Manutenzioni` NEXT (2026-04-09)**: il tab `Dettaglio` di `/next/manutenzioni` non espone piu bottone `Calibra`, modalita calibra, palette forme/target, pulsante `Salva`, marker tecnici o drag/reposition nel ramo embedded usato dal modulo. Il viewer resta una vista pulita del mezzo con tab `Fronte / Sinistra / Destra / Retro`, foto/placeholder della vista attiva e un riepilogo esplicito della manutenzione selezionata; nel domain `nextMappaStoricoDomain` sono stati rimossi anche read/write clone-side degli override tecnici non piu usati dal runtime del dettaglio. Stato modulo: `PARZIALE`.
- **Distinzione esplicita gomme ordinarie vs straordinarie in `Manutenzioni` NEXT (2026-04-09)**: il modulo `/next/manutenzioni` non usa piu un solo sottotipo ambiguo `gomme`. Il form `Nuova / Modifica` distingue ora `Gomme ordinarie per asse` e `Gomme straordinarie`; l'ordinario continua a salvare `gommePerAsse` come stato leggibile per asse, mentre lo straordinario salva un evento clone-side separato tramite `gommeInterventoTipo = straordinario` e `gommeStraordinario`, senza entrare nel calcolo dello stato per asse. Nel tab `Quadro manutenzioni PDF` lo stato gomme ordinario resta in una sezione dedicata `Stato gomme ordinario per asse` e gli interventi straordinari compaiono in una sezione separata `Eventi gomme straordinari`. Stato modulo: `PARZIALE`.
- **Aggiornamento stato gomme per asse `Manutenzioni` NEXT (2026-04-08 tarda notte)**: il modulo `/next/manutenzioni` non usa piu solo un cambio gomme generico nel clone. Il record manutenzione puo ora salvare anche `gommePerAsse` clone-side con `asseId`, `dataCambio` e `kmCambio`; il form `Nuova / Modifica` mostra il cambio gomme come selezione assi + riepilogo per asse, mentre il tab `Quadro manutenzioni PDF` espone per i mezzi motorizzati lo stato finale gomme per asse con `data cambio`, `km cambio`, `km attuali` e `km percorsi`, e per semirimorchi/rimorchi privilegia la data cambio. Il filtro `Attrezzature` e ora presente anche nel quadro. Stato modulo: `PARZIALE`.
- **Aggiornamento runtime `Manutenzioni` NEXT (2026-04-08 tarda sera)**: nel tab `Dettaglio` il ramo embedded non duplica piu il record gia mostrato in `Ultimo intervento mezzo`, perche la lista `Ultime manutenzioni mezzo` esclude ora il record con lo stesso `id`; inoltre `Calibra` e stato portato al flusso richiesto nel clone: selezione target, posizionamento sul click, drag per riposizionare, bottone esplicito `Salva` e rilettura della posizione persistita in override clone-side per `categoria + vista + target`. Stato modulo: `PARZIALE`.
- **Aggiornamento calibrazione reale `Manutenzioni` NEXT (2026-04-08 notte)**: il tab `Dettaglio` deduplica ora il record gia mostrato nel box `Ultimo intervento mezzo`, evitando che compaia di nuovo come primo elemento in `Ultime manutenzioni mezzo`; inoltre `Calibra` non e piu una semplice preview dei pallini ma salva override tecnici clone-side per `categoria mezzo + vista + target`, riletti poi dal viewer tecnico nelle viste `Sinistra/Destra`. Nessuna modifica alla madre o ai writer business; stato modulo: `PARZIALE`.
- **Aggiornamento runtime `Manutenzioni` NEXT (2026-04-08 sera)**: il tab `Dettaglio` usa ora anche il km del record manutenzione aperto e il km corrente derivato dal reader canonico rifornimenti gia usato dal modulo (`readNextRifornimentiReadOnlySnapshot()` tramite `readPageData()` in `NextManutenzioniPage.tsx`) per mostrare, solo sui cambi gomme con dati affidabili, il delta `km ultimo rifornimento - km cambio`; il comando `Calibra` resta pulito nel layout e diventa autoesplicativo via tooltip hover/focus, senza nuove scritte fisse in pagina. Stato modulo: `PARZIALE`.
- **Fase attuale del progetto**: promozione progressiva dei moduli NEXT come nuovo perimetro applicativo del gestionale, con madre legacy intoccabile e scritture reali aperte solo dove il modulo e stato esplicitamente promosso.
- **Stato app legacy**: attiva e riferimento operativo corrente.
- **Stato nuova app next**: `src/next/*` e il nuovo perimetro applicativo in sviluppo; i moduli non vanno piu trattati come clone globalmente `read-only`, ma come superfici da promuovere una alla volta a comportamento madre, mantenendo barrier e controllo documentale sulle scritture.
- **Inversione strategica NEXT (2026-03-10)**: il reset storico verso il clone UX fedele della madre e stato il passaggio che ha stabilizzato il perimetro `src/next/*`; la formulazione originaria `clone read-only globale` non e piu la regola architetturale corrente ed e oggi superata da una promozione modulo per modulo con scritture esplicite e controllate.
- **Stato documentazione**: struttura madre disponibile, documenti core rinominati in italiano, indice e guida di ingresso presenti.
- **Stato processo Codex/report**: `AGENTS.md` e ora la fonte primaria operativa di Codex; `docs/product/REGOLE_LAVORO_CODEX.md` resta supporto sintetico/storico e change report + continuity report restano obbligatori quando il task modifica file del repo.
- **Protocollo sicurezza modifiche**: attivo tramite `docs/product/PROTOCOLLO_SICUREZZA_MODIFICHE.md`; ogni patch deve passare da analisi impatto prima dell'applicazione.
- **Audit generale totale NEXT vs madre (2026-03-30)**: eseguito in sola lettura con report `docs/audit/AUDIT_GENERALE_TOTALE_NEXT_VS_MADRE.md`; il verdetto corrente verificato nel repo e `NO, NEXT non ancora lavorabile in autonomia sul perimetro target`. Le route NEXT ufficiali sono quasi tutte native, ma molti moduli restano `APERTO` perche clone-only, bloccati o con parity esterna non dimostrata.
- **Audit repo vs docs**: eseguito con report dedicato in `docs/audit/`; emerse differenze ad alta priorita su endpoint IA/PDF, policy dati/sicurezza effettive e route legacy ancora attive.
- **Verifica infrastrutturale Firebase/Backend (2026-03-07)**: eseguita in sola lettura; criticita reali confermate su Storage se toccato senza analisi e su canali backend IA/PDF non canonici (`aiCore` non esportata nel repo backend, libretto su Cloud Run esterno); `estraiPreventivoIA` e `stamp_pdf` risultano invece flussi reali da consolidare/documentare.
- **Audit UI/Grafica repo (2026-03-07)**: eseguito in sola lettura; confermate basi forti da riusare per la NEXT (`Dossier`, `CentroControllo`, `Acquisti`, `AutistiInbox/Admin`, `Capo*`) e moduli legacy/transitori da superare o unificare (`Mezzi`, CRUD generici, `MaterialiDaOrdinare` standalone); criticita prevalente architetturale/documentale, non un bug operativo immediato.
- **Blueprint grafico NEXT (2026-03-07)**: creati blueprint visivo, design system, wireframe logici e mappa pattern da riusare in `docs/ui-blueprint/`; la fase successiva sara trasformare questo impianto documentale in shell UI reale della nuova app, lasciando invariata la legacy.
- **Registro ufficiale migrazione NEXT (2026-03-07)**: creato `docs/product/STATO_MIGRAZIONE_NEXT.md`; da ora ogni task futuro che tocca la nuova app deve aggiornarlo per tracciare shell, read-only, scrittura, parti legacy e blocchi, senza perdere contesto tra chat o sessioni.
- **Redesign UI + flusso assi `Manutenzioni` NEXT (2026-04-08)**: `/next/manutenzioni` e allineato alla famiglia visuale approvata del modulo con affinamento mirato del runtime reale: header piu compatto e meno teatrale, barra alta e strip riepilogo mezzo piu precise, tab meno glossy, dashboard piu compatta e superfici operative principali su base chiara continua per form, dettaglio e quadro PDF, mantenendo invece shell esterna, tab e accenti sul tono scuro; gli ultimi micro-fix hanno inoltre corretto in modo piu netto la riga `Data / KM-Ore / Fornitore` del form, reso il materiale la voce principale dell'autosuggest, rimosso completamente la gestione foto dalla tab `Nuova / Modifica` e attivato il nuovo flusso NEXT-only `assiCoinvolti`: la `Nuova manutenzione` salva gli assi selezionati in modo strutturato nella manutenzione, espone ora anche il terzo tipo intervento `Attrezzature` nel form e il tab `Dettaglio`, nelle sole viste `Sinistra/Destra`, carica ora la tavola tecnica reale da `public/gomme` in base alla categoria del mezzo, illumina automaticamente gli assi salvati e riceve in modo esplicito il record manutenzione aperto dalla UI parent; il viewer tecnico non mostra piu marker neutri in vista normale, usa la modalita `Calibra` per preview e grammatica target, e ora rende il comando `Calibra` visibile davvero nel toolbar tecnico, distinguendo assi, fanali/specchi e attrezzature con shape separate senza riaprire il modale gomme legacy e senza toccare `pdfEngine`, madre o runtime legacy.
- **Mappa maestra flussi gestionale (2026-03-07)**: creati `docs/flow-master/MAPPA_MAESTRA_FLUSSI_GESTIONALE.md`, `docs/flow-master/MAPPA_MAESTRA_FLUSSI_GESTIONALE.mmd` e `docs/flow-master/FLUSSI_OPERATIVI_CRITICI.md`; il progetto dispone ora di una vista unificata del funzionamento reale del gestionale prima della shell NEXT.
- **Decisione IA Gestionale per la NEXT (2026-03-07)**: fissata a livello architetturale/documentale; la shell target considera ora 5 macro-aree (`Centro di Controllo`, `Mezzi / Dossier`, `Operativita Globale`, `IA Gestionale`, `Strumenti Trasversali`) e chiarisce che la IA ha doppia natura: area visibile + motore trasversale, senza autorizzare implementazioni automatiche rischiose sulla legacy.
- **Perimetro reale IA v1 per la NEXT (2026-03-07)**: chiarito a livello architetturale/documentale; la prima versione sensata resta `read-only`, limitata inizialmente a `Dossier Mezzo` e `Centro di Controllo`, con obbligo di spiegabilita della risposta (`fonte dati`, `modulo sorgente`, `periodo`, marcatura `DA VERIFICARE` se l'affidabilita non e piena) e separazione netta dalla futura capability di audit tecnico su repo/docs/dati.
- **Backend IA separato con primo adapter reale (2026-03-22)**: il sottosistema `/next/ia/interna*` dispone ora di un primo adapter server-side reale ma `mock-safe` in `backend/internal-ai/server/internal-ai-adapter.js`, con persistenza IA dedicata su file JSON locali in `backend/internal-ai/runtime-data/*`; provider reali, segreti, Firestore/Storage business e backend legacy restano fuori perimetro.
- **Primo provider reale controllato della nuova IA interna (2026-03-22)**: il backend IA separato puo ora agganciarsi a `OpenAI Responses API` solo lato server e solo per preview/proposte IA dedicate; il primo workflow reale `preview -> approvazione -> rollback` resta confinato agli artifact IA e non apre nessuna scrittura business automatica.
- **OpenAI verificato end-to-end nel backend IA separato (2026-03-22)**: `OPENAI_API_KEY` e stata letta davvero lato server dal processo adapter della nuova IA interna, con `gpt-5-mini` attivo su `Responses API`; preview reale, approvazione, rifiuto e rollback sono stati verificati sul solo contenitore IA dedicato, senza segreti lato client e senza toccare dati business.
- **Chat reale controllata + primo repo/UI understanding della nuova IA interna (2026-03-22)**: `/next/ia/interna` puo ora usare la chat server-side reale del backend IA separato con fallback locale esplicito, e il backend puo leggere una snapshot curata e read-only di documenti, route, pattern UI e relazioni tra schermate per spiegare il repository senza diventare un agente che modifica codice.
- **Repo understanding esteso + audit readiness Firebase della nuova IA interna (2026-03-22)**: il backend IA separato costruisce ora anche un indice filesystem controllato di codice/CSS, relazioni curate madre vs NEXT e un audit verificato su cosa manca prima di aprire Firestore/Storage read-only lato server; nessuna lettura business diretta e ancora attiva.
- **Osservatore runtime NEXT passivo + guida integrazione strutturale (2026-03-22)**: la nuova IA interna puo ora usare anche una vista runtime read-only della NEXT, basata su crawl Playwright passivo, screenshot locali e DOM snapshot controllato di route `/next/*` whitelistate; la stessa snapshot espone anche una matrice pratica per suggerire dove integrare pagine, tab, card, modali, bottoni e file candidati senza toccare la madre o aprire bridge business live.
- **Primo hook IA mezzo-centrico governato su Dossier Mezzo (2026-03-22)**: la chat `/next/ia/interna` usa ora un catalogo capability governato sopra il Dossier clone-safe per tradurre richieste libere su singola targa verso stato sintetico mezzo, documenti, costi, libretto, preventivi o report PDF, riusando solo read model NEXT spiegabili e senza allargare il perimetro a retrieval Firebase live.
- **Estensione retrieval Dossier mezzo server-side + capability rifornimenti (2026-03-23)**: il backend IA separato ospita ora anche uno snapshot `Dossier Mezzo` clone-seeded per singola targa, persistito nel contenitore IA dedicato e usato in modo read-only dal hook mezzo-centrico per stato mezzo, costi e rifornimenti; Firebase/Storage business live e verticale `Cisterna` restano fuori perimetro.
- **Governance package backend IA + probe runtime Firebase Admin (2026-03-23)**: `backend/internal-ai/package.json` governa ora anche le dipendenze runtime effettive dell'adapter server-side e il repo include un bootstrap Firebase Admin separato con CLI locale di readiness; il live bridge resta comunque chiuso finche runtime, credenziali e policy non sono davvero verificabili.
- **Ri-verifica bridge live Firebase/Storage IA (2026-03-23)**: eseguita con analisi parallela e controllo locale del repo; il primo bridge business live resta NON apribile in sicurezza. Il backend IA dichiara ora in modo piu stretto il solo boundary futuro ammissibile (`storage/@mezzi_aziendali` + path esatto `librettoStoragePath`), ma resta in stato non attivo finche non esistono credenziale server-side Google dedicata e policy Firestore/Storage verificabili.
- **Tentativo operativo di apertura del live minimo read-only (2026-03-23 sera)**: ri-verificato nel checkout corrente del backend IA separato; il bridge resta NON apribile e il blocco e confermato da piu prerequisiti reali mancanti insieme: nessuna credenziale server-side attiva nel processo (`GOOGLE_APPLICATION_CREDENTIALS`, `FIREBASE_SERVICE_ACCOUNT_JSON`, `FIREBASE_CONFIG` assenti), `firestore.rules` assente, `storage.rules` deny-all in conflitto con l'uso legacy e access layer live dedicato non ancora implementato. Il fallback ufficiale resta il retrieval clone-seeded del `mezzo_dossier`.
- **Verifica finale prerequisiti reali del live minimo IA (2026-03-23)**: il backend IA separato supporta davvero solo lato server `GOOGLE_APPLICATION_CREDENTIALS`, `FIREBASE_SERVICE_ACCOUNT_JSON` e `FIREBASE_CONFIG`, e il runtime locale risolve `firebase-admin`; tuttavia il primo exact read Firestore su `storage/@mezzi_aziendali` e il primo exact file read Storage da `librettoStoragePath` restano NON attivabili nel processo reale corrente per tre blocchi verificati: nessuna credenziale server-side presente, assenza di `firestore.rules` versionate e access layer live dedicato ancora non aperto.
- **Deep runtime observer NEXT + selettore formato output evoluto (2026-03-22)**: la nuova IA interna osserva ora la NEXT in modo piu profondo ma sempre read-only, con 19 route reali, 23 screenshot, 4 stati whitelist-safe e route dinamiche dossier/analisi/gomme/rifornimenti; inoltre la chat sceglie e dichiara meglio se rispondere nel thread, aprire un report PDF o formulare una proposta di integrazione stabile nella NEXT.
- **Copertura runtime UI NEXT quasi completa ma onesta (2026-03-23)**: dopo il micro-task mirato sui gap del Prompt 59, l'observer read-only della nuova IA interna cataloga 53 route candidate della NEXT e ne osserva davvero 53 con 78 screenshot runtime; gli stati interni osservati salgono a 25 su 26 tentati. Resta un solo stato non osservabile nel perimetro sicuro: la modale `Home -> Vedi tutto`, il cui trigger e visibile ma disabilitato dal guard rail read-only del clone.
- **Readiness Firebase tipizzata con whitelist candidate della nuova IA interna (2026-03-22/2026-03-23)**: il backend IA separato espone prerequisiti condivisi e whitelist candidate non attive per un futuro bridge Firestore/Storage read-only; oggi il package dedicato governa le dipendenze runtime dell'adapter, il runtime locale risolve `firebase-admin` e il bootstrap supporta `GOOGLE_APPLICATION_CREDENTIALS`, `FIREBASE_SERVICE_ACCOUNT_JSON` e `FIREBASE_CONFIG`, ma restano bloccanti reali credenziali server-side dedicate, `firestore.rules` assente, `storage.rules` in conflitto e access layer live dedicato non ancora aperto.
- **Reality check finale sul live minimo IA (2026-03-23)**: la ri-verifica finale del bridge read-only minimo conferma che il live NON e apribile oggi. Nel processo corrente non risultano `GOOGLE_APPLICATION_CREDENTIALS`, `FIREBASE_CONFIG`, `GOOGLE_CLOUD_PROJECT`, `GCLOUD_PROJECT` o `FIREBASE_SERVICE_ACCOUNT_JSON`; inoltre, pur con bootstrap dedicato e runtime `firebase-admin` risolvibile, mancano ancora policy Firestore verificabili e un access layer live stretto per `storage/@mezzi_aziendali` e `librettoStoragePath`.
- **Report IA in anteprima PDF reale nel clone (2026-03-22)**: `/next/ia/interna` separa ora davvero chat e documento; i report strutturati vengono salvati come artifact IA e aperti in una modale con PDF reale generato on demand, senza riversare il contenuto lungo nel thread.
- **Governance package del backend IA separato (2026-03-22)**: `backend/internal-ai/package.json` esiste ora come package dedicato del perimetro server-side IA; il bridge Firebase/Storage business read-only resta comunque non attivo finche non esistono `firebase-admin` governato dal package, credenziali server-side dedicate e policy verificabili.
- **Shell reale NEXT nel runtime (2026-03-07)**: implementata nel frontend con route dedicate `/next/*`, layout separato, 5 macro-aree placeholder navigabili e nessuna nuova scrittura dati; la legacy resta raggiungibile e invariata sulle route attuali.
- **Visibilita/accesso ruolo nella shell NEXT (2026-03-08)**: predisposta una struttura frontend centralizzata per ruoli `admin`, `gestionale` e `autista`, con menu dinamico, guardie route leggere, simulazione tecnica via query param `role` e separazione esplicita dell'esperienza autista, senza auth reale o impatto sulla legacy.
- **Quinta area reale NEXT oltre il placeholder generico (2026-03-08)**: la macro-area `/next/strumenti-trasversali` e stata trasformata in una shell UI strutturata, `read-only`, che chiarisce il ruolo dei servizi condivisi della piattaforma, dei PDF standard, delle utility comuni e del confine rispetto a `IA Gestionale`, senza attivare ancora servizi runtime o tool legacy.
- **Quarta area reale NEXT oltre il placeholder generico (2026-03-08)**: la macro-area `/next/ia-gestionale` e stata trasformata in una shell UI strutturata, `read-only`, che chiarisce missione dell'assistente business, perimetro v1, superfici iniziali `Dossier` + `Centro di Controllo`, spiegabilita obbligatoria e separazione dalla `IA Audit Tecnico`, senza integrare ancora modelli, backend o dati runtime.
- **Terza area reale NEXT oltre il placeholder generico (2026-03-08)**: la macro-area `/next/operativita-globale` e stata trasformata in una shell UI strutturata, `read-only`, che chiarisce domini globali non mezzo-centrici, confine con il Dossier, collocazione futura di `Acquisti & Magazzino` e rapporto con IA futura, senza importare ancora logiche operative legacy.
- **Seconda area reale NEXT oltre il placeholder generico (2026-03-08)**: la macro-area `/next/centro-controllo` e stata trasformata in una shell UI strutturata, `read-only`, che chiarisce cabina di regia, priorita, alert, scadenze, convergenza dei flussi e collegamento con Dossier e IA futura, senza importare ancora logiche della Home legacy.
- **Prima area reale NEXT oltre il placeholder generico (2026-03-08)**: la macro-area `/next/mezzi-dossier` e stata trasformata in una shell UI strutturata, `read-only`, che chiarisce ingresso area mezzi, centralita del Dossier, convergenze mezzo-centriche e distinzione da `Operativita Globale`, senza importare ancora logiche business legacy.
- **Primo import reale dati nella NEXT (2026-03-08)**: `/next/mezzi-dossier` legge ora `storage/@mezzi_aziendali` tramite reader canonico dedicato al dominio `Anagrafiche flotta e persone`, limitato a campi stabili (`id`, `targa`, `categoria`, `marca`, `modello`, `autistaNome`), senza scritture e senza importare ancora il Dossier completo.
- **Primo Dossier Mezzo NEXT iniziale (2026-03-08)**: `/next/mezzi-dossier/:targa` espone ora un dettaglio mezzo `read-only` basato solo sul dominio `Anagrafiche flotta e persone`; mostra identita mezzo, stato di importazione e convergenze future, senza leggere ancora lavori, rifornimenti, documenti o costi.
- **Primo blocco tecnico reale nel Dossier NEXT (2026-03-08)**: il dettaglio `/next/mezzi-dossier/:targa` converge ora anche una porzione minima `read-only` del dominio `Operativita tecnica mezzo`, tramite reader canonico dedicato su `@lavori` e `@manutenzioni`, limitato a backlog lavori, lavori chiusi e manutenzioni essenziali per `targa`, senza writer, materiali, costi o ricostruzioni complete della logica legacy.
- **Layer `D04` di ricostruzione controllata NEXT (2026-03-09)**: il Dossier Mezzo NEXT legge ora i rifornimenti tramite un solo layer dedicato `D04`, confinato nella NEXT, che usa internamente `@rifornimenti` e `@rifornimenti_autisti_tmp`, normalizza shape legacy, ricostruisce autista/badge/km/costo/timestamp quando possibile e consegna al Dossier solo un modello pulito con provenienza e qualita del dato.

- **Reset prodotto della pagina IA interna (2026-03-23)**: `/next/ia/interna` e stata riallineata come chat unica stile ChatGPT, con composer unico, allegati IA-only nello stesso thread, memoria repo/UI usata nelle richieste libere quando disponibile, output/report/PDF relegati a strumenti secondari e pannelli tecnici collassabili; nessuna madre toccata, nessuna scrittura business, nessun bridge Firebase/Storage live riaperto.
- **Base universale chat/IA clone-NEXT (2026-03-26)**: `/next/ia/interna` non e piu solo console verticale. Esiste ora un primo registry totale del clone/NEXT con `10` moduli, `30` route, `11` adapter, `13` capability IA censite, `8` gia assorbite, entity resolver, request resolver, orchestrator, composer unico e router documenti; D03, D04, D05, D06, D07/D08, D10 e repo-understanding vengono da adesso trattati come adapter specializzati sotto un gateway universale e non piu come prodotto finale a isole.
- **Chiusura gap operativi del gateway universale (2026-03-26)**: il sistema universale emette ora handoff standard `iaHandoff`, prefill canonico, inbox documentale universale, route target con payload uniforme, conformance gate runtime per moduli futuri e scenari E2E documentati su libretto, preventivo fornitore, documento ambiguo, mezzo, autista, cisterna e materiali.
- **Chiusura operativa del perimetro universale corrente (2026-03-26)**: i moduli target attuali del clone/NEXT consumano ora davvero `iaHandoff`, applicano prefill reale, aggiornano lo stato consumo nel repository IA interno e chiudono i casi E2E principali fino al punto corretto della UI; non restano gap aperti nel perimetro operativo corrente del sistema universale.
- **Prompt 33 - parita clone/NEXT portata avanti davvero (2026-03-29)**: il clone non e ancora `100%` chiuso, ma le route ufficiali `Mezzi`, `Gestione Operativa`, `Inventario`, `Materiali consegnati`, `Attrezzature cantieri`, `Manutenzioni`, `Ordini`, `Dettaglio ordine`, `Lavori` e `Dettaglio lavoro` montano ora la UI madre sopra un bridge legacy-shaped pulito derivato dai domain NEXT; i blocchi residui reali sono concentrati dove la madre incorpora ancora Firestore/Storage diretto o workflow non reimportabili 1:1 senza toccare file fuori perimetro.
- **Prompt 34 - chiusure residue replicate nel clone (2026-03-29)**: `Dossier Lista`, `Colleghi`, `Fornitori`, `IA Home` e `IA API Key` sono stati riallineati nel clone con UI madre-equivalente e letture da layer NEXT puliti (`D01`, `D05`, `D11`), senza toccare la madre; i residui veri restano concentrati sui moduli piu grandi che richiedono ancora replica strutturale dentro `src/next/*`.
- **Prompt 35 - hardening finale residuo clone-side (2026-03-29)**: il clone estende il boundary dati NEXT anche ai flussi autisti, a `Home`, a `Centro di Controllo`, alle child route IA legacy che leggono `storageSync` e al backbone dossier/analisi; in questo run la sola nuova chiusura piena e `Libretti Export`, mentre i moduli che restano appoggiati a `getDoc/getDocs/setDoc/addDoc/uploadBytes` dentro la madre restano dichiarati aperti.
- **Prompt 36 - ricostruzione reale del Centro di Controllo NEXT (2026-03-29)**: `/next/centro-controllo` non monta piu la pagina madre wrapperizzata; usa una pagina clone vera che replica tabs, priorita, tabelle e preview PDF della madre, ma legge solo i layer `D01`, `D03` e `D04` del clone.
- **Prompt 38 - svuotamento backlog residuo su Dossier/Analisi/Materiali (2026-03-29)**: `Dossier Mezzo`, `Analisi Economica` e `Materiali da ordinare` non montano piu il runtime madre; usano pagine NEXT vere sopra il composite dossier `D01/D02/D04/D07-D08` e un overlay procurement clone-only che riassorbe gli ordini confermati senza scrivere su `@ordini`.
- **Prompt 39 - chiusura ultimi 8 moduli residui NEXT (2026-03-29)**: il backlog residuo del clone/NEXT e stato chiuso anche su `Acquisti / Preventivi / Listino`, `IA Libretto`, `IA Documenti`, `IA Copertura Libretti`, `Cisterna`, `Cisterna IA`, `Cisterna Schede Test` e `Autisti / Inbox`; le route ufficiali non montano piu il runtime madre, il procurement legge ora anche `@preventivi`, `@preventivi_approvazioni` e `@listino_prezzi` tramite `D06`, le pagine IA child e `Cisterna*` risultano native su layer NEXT e il perimetro `Autisti / Inbox` usa copie NEXT vere con bridge clone-safe dedicati per letture/scritture locali non distruttive.
- **Prompt 50 - audit generale totale NEXT vs madre (2026-03-30)**: i claim di chiusura complessiva del backlog non vengono confermati dal codice reale. Moduli come `Home`, `Mezzi`, `Dossier Mezzo`, `Inventario`, `Materiali`, `Procurement`, `Lavori`, `Capo Costi`, `IA documentale`, `Cisterna`, `Colleghi`, `Fornitori`, `Autisti / Inbox` e `Manutenzioni` risultano ancora `APERTO`; i soli moduli promossi a `CHIUSO` con prova diretta in questo audit sono `Gestione Operativa`, `IA Home` e `IA API Key`.

## 2. Decisioni architetturali confermate
- Dal `2026-03-10` e sospesa la strategia NEXT precedente basata su shell autonoma, import dominio-prima-pagina e divieto di clone UX legacy.
- Dal `2026-03-10` il progetto ha usato un reset verso clone fedele della madre per stabilizzare `src/next/*`; quella fase `read-only` iniziale resta un fatto storico, ma non e piu la regola globale corrente.
- Regola architetturale corrente: `src/next/*` e il nuovo perimetro applicativo del gestionale; la madre resta intoccabile; le scritture reali si aprono modulo per modulo, solo sui dataset e sulle operazioni necessari, con `cloneWriteBarrier.ts` e documentazione allineata.
- Nuova app in parallelo alla legacy.
- Stessa base dati iniziale (senza duplicazione DB in partenza).
- La fase iniziale della NEXT e stata `read-only`; oggi il modello corretto e promozione progressiva dei moduli con apertura controllata delle scritture.
- Home = Centro di Controllo.
- Dossier Mezzo = cuore del sistema.
- Moduli globali separati dai flussi targa-centrici.
- IA Gestionale / Assistente Gestionale come macro-area visibile della NEXT.
- IA integrata anche come funzione trasversale.
- IA Business NEXT v1 = `read-only`.
- Superfici iniziali IA Business NEXT v1 = `Dossier Mezzo` + `Centro di Controllo`.
- Risposta IA v1 con spiegabilita obbligatoria: fonte dati, modulo sorgente, periodo e marcatura `DA VERIFICARE` quando necessario.
- IA Business runtime della NEXT separata da una futura capability distinta di `IA Audit Tecnico` su repo/docs/dati.
- NEXT runtime visibile su route separate `/next/*`, senza sostituire `/` o alterare il comportamento delle route legacy.
- Predisposizione frontend NEXT per visibilita/accesso ruolo: `admin` vede tutte le macro-aree shell, `gestionale` vede solo aree abilitate dal preset tecnico, `autista` resta esperienza separata e non entra nella shell admin come utente ridotto.
- PDF gestito come funzione trasversale.
- Distinzione tra PDF standard tecnico e PDF intelligenti / report assistiti in area IA Gestionale.
- Area Autisti separata da area Admin.
- Modello permessi a 3 livelli (Super Admin / Account gestionale / Autista): **DA VERIFICARE** come conferma finale operativa.
- Governance IA: la IA segnala e motiva, l'utente decide, ChatGPT analizza/struttura, Codex applica; nessuna patch autonoma o nuova scrittura rischiosa autorizzata in questa fase.

## 3. Documenti principali da leggere
1. `docs/LEGGI_PRIMA.md`
2. `docs/STRUTTURA_COMPLETA_GESTIONALE.md`
3. `docs/architecture/NUOVA_STRUTTURA_GESTIONALE.md`
4. `docs/data/MAPPA_COMPLETA_DATI.md`
5. `docs/security/SICUREZZA_E_PERMESSI.md`
6. `docs/product/REGISTRO_PUNTI_DA_VERIFICARE.md`
7. `docs/product/STATO_MIGRAZIONE_NEXT.md` per qualsiasi task che tocca la nuova app NEXT

## 4. Punti aperti da non dimenticare
- Stream eventi autisti canonico (`@storico_eventi_operativi` vs `autisti_eventi`).
- Contratto finale allegati preventivi (`preventivi/ia/*` vs `preventivi/<id>.pdf`).
- Matrice ruoli/permessi definitiva (distinzione admin/capo/account gestionale).
- Policy Firestore effettive (file `firestore.rules` non presente nel repo).
- Governance finale endpoint IA multipli.
- Standard UI canonico cross-modulo per la NEXT (ora definito a livello documentale, da tradurre in shell UI reale).
- Hardening finale delle dipendenze shared non critiche ancora riusate dal clone su CSS/helper locali, senza riaprire wrapper madre o accessi raw sostanziali.
- Dettaglio e stato aggiornato: `docs/product/REGISTRO_PUNTI_DA_VERIFICARE.md`.
- Esito dettagliato verifica infrastrutturale: `docs/audit/VERIFICA_INFRASTRUTTURA_FIREBASE_BACKEND.md`.
- Esito dettagliato audit UI repo: `docs/ui-audit/AUDIT_GRAFICA_ATTUALE.md`.
- Esito dettagliato blueprint UI NEXT: `docs/ui-blueprint/BLUEPRINT_GRAFICO_NEXT.md`.

## 5. Ultimi avanzamenti importanti
- Creata documentazione madre completa del progetto.
- Rinomina documenti core in italiano per maggiore leggibilita.
- `AGENTS.md` creato/aggiornato come guida operativa permanente.
- Consolidato `AGENTS.md` come fonte primaria operativa Codex, con workflow rapido, formato corto dei task futuri e richiamo esplicito alla checklist unica per i task IA.
- Struttura `change-reports` creata con regole e template.
- Struttura `continuity-reports` creata con regole e template.
- Creato `REGISTRO_PUNTI_DA_VERIFICARE` per memoria fissa dei temi aperti.
- Completata verifica reale Firebase/Backend con chiarimento di rischi immediati, futuri e solo documentali.
- Completato audit UI/grafica del repo con distinzione tra pattern da mantenere, rifinire, unificare o superare per la progettazione NEXT.
- Creato il blueprint grafico ufficiale della NEXT con design system e wireframe logici, pronto per guidare la futura costruzione della shell UI reale.
- Creato il registro ufficiale di migrazione della NEXT per tracciare in modo permanente cosa e shell, quali moduli sono ancora solo in lettura, quali sono stati promossi in scrittura controllata, cosa resta legacy e cosa e ancora da verificare.
- Creata la mappa maestra dei flussi del gestionale con evidenza delle convergenze verso Dossier, moduli globali, capability trasversali e flussi critici.
- Formalizzata la decisione architetturale che introduce `IA Gestionale` come macro-area della NEXT con ruolo sia dedicato sia trasversale, mantenendo invariata la legacy.
- Formalizzato anche il perimetro reale della `IA Gestionale` v1: rollout progressivo, `read-only`, prime superfici `Dossier Mezzo` e `Centro di Controllo`, separazione dalla capability di audit tecnico su repository/documentazione/dati.
- Creata la shell reale della NEXT nel runtime applicativo con route dedicate `/next/*`, macro-aree navigabili, layout separato e placeholder coerenti con blueprint e design system, senza impattare i moduli legacy.
- Aggiunta nella shell NEXT una prima struttura solida di visibilita/accesso per ruolo, con simulazione tecnica `admin` / `gestionale` / `autista`, menu condizionale, blocco route non consentite e vista autista separata solo lato frontend.
- Trasformata `/next/strumenti-trasversali` nel quinto caso di area NEXT realmente strutturata oltre il placeholder iniziale, come shell `read-only` che esplicita servizi condivisi, PDF standard, utility comuni, richiami cross-area e confine netto rispetto a `IA Gestionale`.
- Trasformata `/next/ia-gestionale` nel quarto caso di area NEXT realmente strutturata oltre il placeholder iniziale, come shell `read-only` che esplicita missione, perimetro v1, spiegabilita obbligatoria, limiti iniziali e separazione dalla futura `IA Audit Tecnico`.
- Trasformata `/next/operativita-globale` nel terzo caso di area NEXT realmente strutturata oltre il placeholder iniziale, come shell workflow globale che distingue domini condivisi, confine con Dossier e collocazione futura di `Acquisti & Magazzino`.
- Trasformata `/next/centro-controllo` nel secondo caso di area NEXT realmente strutturata oltre il placeholder iniziale, come cockpit `read-only` che esplicita priorita, alert, scadenze, destinazioni modulo e spazio futuro per l'IA Business v1.
- Trasformata `/next/mezzi-dossier` nel primo caso di area NEXT realmente strutturata oltre il placeholder iniziale, con shell `detail-first` centrata sul Dossier e predisposta ai futuri import `read-only`.
- Avviato il primo import reale dati della NEXT sul dominio `Anagrafiche flotta e persone`: `/next/mezzi-dossier` espone ora un elenco mezzi `read-only` basato su `storage/@mezzi_aziendali`, con reader canonico dedicato e senza introdurre scritture o reader improvvisati da chiavi sparse.
- Attivato il primo Dossier Mezzo NEXT iniziale: dall'elenco mezzi si apre ora un dettaglio `read-only` su route dedicata `/next/mezzi-dossier/:targa`, sempre basato solo sul dominio stabile `D01` e costruito per preparare le future convergenze verso il Dossier senza clonare la legacy.
- Attivata la prima convergenza tecnica reale del Dossier NEXT: lo stesso dettaglio mezzo legge ora anche una porzione minima e controllata di `D02 Operativita tecnica mezzo`, tramite reader canonico dedicato su `@lavori` e `@manutenzioni`, mantenendo il Dossier `read-only` e separato da writer, materiali e costi legacy.
- Attivato il layer `D04` di ricostruzione controllata NEXT sul dominio `Rifornimenti e consumi`: il Dossier Mezzo legge ora un blocco rifornimenti `read-only` che usa internamente dataset business e feed campo legacy, ma espone in pagina solo un modello pulito unico con provenienza e qualita del dato, senza modificare il madre.
- Portato il sottosistema IA interna oltre il solo scaffolding in-process: esiste ora un primo adapter server-side reale ma mock-safe, con persistenza IA dedicata per artifact, memoria operativa e traceability minima su file JSON locali separati dai dataset business.
- Aperto anche il primo retrieval server-side controllato del backend IA separato: oggi legge solo uno snapshot read-only D01 seedato dal clone per la capability `libretto-preview`, senza Firestore/Storage business diretti e con fallback locale esplicito.
- Aperto anche il primo workflow reale con provider lato server del backend IA separato: oggi genera solo sintesi guidate di report gia letti, chiede approvazione esplicita, traccia il rollback e resta confinato al contenitore IA dedicato senza applicare mutazioni business.
- Esteso il backend IA separato alla chat reale controllata e a un primo livello di comprensione repo/UI: la chat usa ora OpenAI solo lato server quando il processo adapter eredita `OPENAI_API_KEY`, mentre il retrieval repo/UI legge una snapshot curata e read-only di documenti, route, macro-aree e pattern visivi rappresentativi della NEXT.
- Esteso ulteriormente il repo understanding del backend IA separato: la snapshot repo/UI include ora anche un indice controllato di file codice/CSS, relazioni madre vs NEXT e un audit di readiness Firestore/Storage utile a capire in modo verificato cosa manca prima di aprire letture server-side business read-only.
- Rifinita l'esperienza di `/next/ia/interna` come chat interna piu chiara e usabile: input naturale, thread leggibile, stati backend piu comprensibili e report strutturati separati dal thread in artifact/modale PDF reale generata nel perimetro IA.
- Resa piu seria la governance readiness del backend IA separato: oltre alla snapshot repo/UI con requisiti condivisi e whitelist candidate non attive (`storage/@mezzi_aziendali` e path esatto da `librettoStoragePath`), esiste ora un package dedicato `backend/internal-ai/package.json`; il bridge Firestore/Storage business read-only resta comunque non ancora sicuro da aprire.
- Resa piu seria anche la governance del package backend IA separato: il package dedicato dichiara ora le dipendenze runtime effettive dell'adapter, esiste un bootstrap Firebase Admin separato con CLI locale di readiness, ma il live bridge resta comunque bloccato finche runtime, credenziali e policy non sono davvero verificabili.
- Esteso il perimetro credenziali server-side del backend IA separato (2026-03-23): il codice supporta ora anche `FIREBASE_SERVICE_ACCOUNT_JSON` oltre a `GOOGLE_APPLICATION_CREDENTIALS` e `FIREBASE_CONFIG`, ma il primo bridge live minimo resta NON apribile nel checkout corrente per credenziali assenti nel processo, `firestore.rules` assente, `storage.rules` deny-all in conflitto e access layer live non ancora implementato.
- Ri-verificato il primo bridge live minimo della nuova IA interna con controllo ambiente/processo reale: il fallback ufficiale resta il retrieval clone-seeded del `mezzo_dossier` perche nel checkout corrente mancano sia credenziali server-side dimostrate sia il runtime dedicato `backend/internal-ai` materializzato, oltre alle policy Firestore/Storage gia note come bloccanti.
- Aggiunto un observer runtime passivo della NEXT: `npm run internal-ai:observe-next` visita solo route `/next/*` whitelistate, salva screenshot e DOM snapshot nel contenitore IA dedicato e permette alla nuova IA di motivare meglio dove integrare funzioni future tramite modulo, superficie UI e file candidati.
- Portata la copertura runtime della NEXT al massimo oggi verificabile in read-only: `52/53` route osservate, `70` screenshot, `18/26` stati interni osservati, pannello IA non piu troncato ai primi elementi e payload chat repo/UI esteso a tutta la mappa runtime compatta con relazioni `schermata -> file/modulo/flusso`.
- Aggiunto il primo hook IA mezzo-centrico governato sul Dossier: la chat puo ora tradurre richieste libere per singola targa in capability spiegabili su stato mezzo, documenti, costi, libretto, preventivi o report PDF, senza introdurre retrieval business live o nuova logica sparsa nelle pagine.
- Esteso il primo hook IA mezzo-centrico con uno snapshot `Dossier Mezzo` server-side clone-seeded per singola targa: il backend IA separato puo ora servire stato mezzo, riepilogo costi e riepilogo rifornimenti sopra il Dossier read-only, mantenendo fallback locale esplicito; `Cisterna` resta solo verticale specialistico e i bridge Firebase/Storage business live restano non attivi.
- Ri-verificata la possibilita di aprire un primo bridge Firebase/Storage business live della nuova IA interna: il verdetto operativo resta negativo, ma il repo dichiara ora un boundary futuro piu stretto e machine-readable sul solo perimetro `storage/@mezzi_aziendali` + `librettoStoragePath`, senza riaprire il live in modo sporco.
- Costruita la prima base universale reale della chat/IA nel clone/NEXT: registry totale, contract standard adapter, entity model, request resolver, orchestrator, composer e router documenti esistono ora sia come runtime locale sia come documentazione permanente, e le capability IA gia deployate vengono censite come sorgenti riusabili del sistema universale.
- Completato l'audit totale di parita clone/NEXT vs madre: il repo dimostra che il clone non e ancora `100%` uguale alla madre, che i soli casi oggi chiaramente `PARI` sono `Dossier Gomme` e `Dossier Rifornimenti`, che molti moduli ufficiali restano `PARI MA RAW` e che i nodi principali da chiudere sono `Centro di Controllo`, `Mezzi + Dossier Mezzo`, `Procurement`, `Lavori` e `Autisti Inbox`; `Targa 360` e `Autista 360` restano `FUORI PERIMETRO`.
- Portata avanti una chiusura runtime reale del clone sul perimetro dove la madre poteva essere mantenuta: il clone legge ora tramite bridge legacy-shaped pulito anche `@mezzi_aziendali`, `@colleghi`, `@inventario`, `@materialiconsegnati`, `@attrezzature_cantieri`, `@ordini`, `@lavori` e `@manutenzioni` nelle route ufficiali convertite.
- Chiuse altre 5 superfici clone senza uscire da `src/next/*`: `Dossier Lista`, `Colleghi`, `Fornitori`, `IA Home` e `IA API Key` ora replicano il comportamento madre sopra reader NEXT dedicati, lasciando bloccato solo il confine finale delle scritture nel clone `read-only`.
- Esteso il bridge legacy-shaped anche al perimetro autisti (`sessioni`, `eventi`, `segnalazioni`, `controlli`, `richieste attrezzature`, `rifornimenti`) e riagganciato `Home` e `Centro di Controllo` ai reader clone-side quando passano da `storageSync`.
- Portato `Libretti Export` a parita piena: la route ufficiale `/next/libretti-export` monta ora la pagina madre vera sopra il boundary `flotta`, quindi UI e comportamento restano quelli della madre mentre il dataset `@mezzi_aziendali` passa dal layer NEXT pulito.
- Riagganciate anche `IA Libretto`, `IA Documenti`, `IA Copertura Libretti`, `Dossier Mezzo` e `Analisi Economica` ai boundary dati NEXT dove usano ancora `storageSync`; restano pero aperte perche il runtime madre continua a usare accessi Firestore/Storage diretti non ancora sostituiti nel clone.
- Ricostruito davvero il `Centro di Controllo` nel clone: la route ufficiale usa ora `NextCentroControlloParityPage`, replica la pagina madre senza montarla e consuma solo read model NEXT puliti per manutenzioni, rifornimenti e flussi autisti.
- Ricostruita davvero anche `Home` nel clone: `/next` non monta piu `src/pages/Home.tsx`, ma usa una pagina NEXT vera che replica card, alert, modali prenotazione/pre-collaudo/revisione, export PDF e collegamenti rapidi sopra i reader `D10`/`D03`, con overlay clone-only per ack alert e modifiche locali di prenotazioni e luoghi mezzo.
- Riallineato anche il blocco `Capo`: le route ufficiali `/next/capo/mezzi` e `/next/capo/costi/:targa` sono ora madri-like fuori ma completamente NEXT dentro, con approvazioni clone-only locali, anteprima PDF preventivi, anteprima PDF documento e preview timbrato, senza ricadere nel runtime legacy.
- Ricostruiti nel clone anche `Dossier Mezzo`, `Analisi Economica` e `Materiali da ordinare`: le tre route ufficiali non montano piu `src/pages/*` come soluzione finale e usano ora pagine NEXT vere sopra il composite dossier `D01/D02/D04/D07-D08`, `nextDossierCloneState` e un overlay procurement clone-only che non scrive sulla madre.
- Formalizzata la procedura ufficiale `madre -> clone/NEXT` in `docs/architecture/PROCEDURA_MADRE_TO_CLONE.md` e usata subito come contratto operativo per chiudere il backlog reale aperto dall'audit finale, compreso l'ultimo hardening critico di `Autisti / Inbox` su adapter NEXT locali e bridge admin clone-only.

## 6. Regola operativa obbligatoria
Prima di ogni nuovo task bisogna leggere almeno:
1. `docs/STATO_ATTUALE_PROGETTO.md`
2. `docs/STRUTTURA_COMPLETA_GESTIONALE.md`
3. `docs/data/MAPPA_COMPLETA_DATI.md`
4. `docs/product/REGISTRO_PUNTI_DA_VERIFICARE.md`
5. `AGENTS.md`

Per ogni nuova patch e obbligatorio applicare anche:
6. `docs/product/PROTOCOLLO_SICUREZZA_MODIFICHE.md`

Se il task tocca la NEXT, bisogna inoltre leggere e aggiornare:
7. `docs/product/STATO_MIGRAZIONE_NEXT.md`

## 7. Prossimo passo consigliato
Eseguire un audit separato della chiusura esecutiva prompt 42 prima di promuovere la NEXT a `autonoma sul perimetro target`, mantenendo `/next/ia/interna` come cervello operativo del clone/NEXT e senza riaprire scritture business o live-read.

## 8. Stato documento
- **STATO: CURRENT**

## 9. Aggiornamento 2026-04-08 - micro-fix UI riga metriche `Manutenzioni` NEXT
- Nella tab `Nuova / Modifica` del modulo `/next/manutenzioni` e stata corretta solo la resa della riga `Data / KM-Ore / Fornitore`.
- La griglia usa ora tre colonne piu leggibili e separate, con `Fornitore` prioritario nello spazio disponibile.
- La card `Mezzo attivo` ha piu stacco verticale dal blocco `Campi base`, senza toccare logica, routing o dati.
- Stato modulo:
  - `Manutenzioni` -> `PARZIALE`

## 10. Aggiornamento 2026-04-08 - riga metriche `Manutenzioni` resa a mini-card
- La riga `Data / KM-Ore / Fornitore` della tab `Nuova / Modifica` non usa piu tre soli input affiancati.
- I tre campi sono ora montati come mini-card separate vere con bordo e gap autonomi:
  - `Data`
  - `KM/Ore`
  - `Fornitore`
- La card `Mezzo attivo` e stata rialzata in modo piu percepibile e ha piu aria sopra `Campi base`.
- Nessuna modifica a logica dati, salvataggio, PDF, foto o dettaglio.

## 11. Aggiornamento 2026-04-08 - riga metriche `Manutenzioni` ricompattata
- La soluzione a tre mini-card alte e stata rimossa dalla tab `Nuova / Modifica`.
- La riga `Data / KM-Ore / Fornitore` usa ora tre field-group compatti e separati:
  - `Data` corto
  - `KM/Ore` corto
  - `Fornitore` lungo e flessibile
- Ridotta l'altezza complessiva della riga eliminando wrapper beige alti e padding verticali superflui.
- `Mezzo attivo` resta rialzato e con piu stacco da `Campi base`.
- Nessuna modifica a logica dati, routing, PDF, foto o dettaglio.

## 12. Aggiornamento 2026-04-08 - sostituzione definitiva blocco metriche `Manutenzioni`
- Il blocco `Data / KM-Ore / Fornitore` della tab `Nuova / Modifica` e stato sostituito in modo definitivo.
- Struttura runtime finale:
  - wrapper `man2-metric-row`
  - `man2-metric-group man2-metric-group--date`
  - `man2-metric-group man2-metric-group--metric`
  - `man2-metric-group man2-metric-group--supplier`
- Colonne desktop fissate a `180px 180px minmax(360px, 1fr)` con `gap: 16px`.
- Nessuna modifica a logica dati, salvataggio, PDF, foto o dettaglio.

## 13. Aggiornamento 2026-04-08 - stile input metriche riallineato
- I tre campi `Data / KM-Ore / Fornitore` nella tab `Nuova / Modifica` riusano ora la stessa base visiva dei campi corretti del form, in particolare `Tipo` e `Sottotipo`.
- Nessuna modifica a layout, colonne, gap o struttura della riga.
- Riallineati font, bordo, radius, padding e placeholder al pattern gia usato nel modulo.

## 14. Aggiornamento 2026-04-08 - micro-fix header e riga metriche `Manutenzioni`
- Ridotto il rumore visivo della testata spostando leggermente il contenuto utile del modulo verso destra, cosi l'icona menu flottante incide meno sulla fascia alta.
- Rimossa la label piccola sopra il titolo `Nuova manutenzione`.
- Micro-ribilanciata la riga `Data / KM-Ore / Fornitore` dando un po' piu respiro al campo centrale e ritardando l'inizio visivo del `Fornitore`.
- Nessuna modifica a logica dati, routing, PDF, foto o dettaglio.

## 15. Aggiornamento 2026-04-08 - larghezze fisse riga metriche `Manutenzioni`
- La riga `Data / KM-Ore / Fornitore` dentro `Campi base` usa ora larghezze desktop fisse e compatte:
  - `Data`: `190px`
  - `KM/Ore`: `140px`
  - `Fornitore`: `260px`
- Il blocco non usa piu `1fr` sul fornitore e non si allunga a riempire tutto il contenitore.
- Gap confermato uniforme a `16px`.
- Nessuna modifica a logica dati, routing, PDF, foto o dettaglio.

## 16. Aggiornamento 2026-04-08 - viewer tecnico `Dettaglio` ripulito in `Manutenzioni`
- Nel tab `Dettaglio`, sulle viste tecniche `Sinistra/Destra`, la modalita normale mostra ora solo immagine tecnica + highlight reali degli assi salvati: nessun marker neutro resta visibile se la manutenzione aperta non ha target strutturati.
- `Calibra` e ora l'unico contesto dove compaiono preview asse e grammatica target del viewer tecnico.
- `src/next/mezziHotspotAreas.ts` espone una tassonomia reale `targetKind` (`assi`, `fanali_specchi`, `attrezzature`) usata per differenziare shape e stato visivo.
- Assi/gomme, fanali/specchi e attrezzature non condividono piu lo stesso marker grafico.
- Nessuna modifica a writer business, PDF, madre o runtime legacy.

## 17. Aggiornamento 2026-04-08 - binding esplicito record -> viewer tecnico in `Manutenzioni`
- Il tab `Dettaglio` non usa piu fallback impliciti alla "ultima manutenzione con assiCoinvolti".
- `src/next/NextManutenzioniPage.tsx` mantiene ora un `selectedDetailRecordId` esplicito e passa al viewer il record realmente aperto nella UI parent.
- `src/next/NextMappaStoricoPage.tsx` consuma il record selezionato come unica sorgente per `assiCoinvolti` e per la label di contesto del viewer tecnico.
- Se il record aperto non ha `assiCoinvolti`, il viewer tecnico resta pulito.
- Le liste `Ultimi interventi` e `Ultime manutenzioni` possono ora cambiare esplicitamente il record aperto senza ambiguita.

## 18. Aggiornamento 2026-04-08 - visibilita runtime `Attrezzature` + `Calibra` in `Manutenzioni`
- Audit runtime locale eseguito sul codice reale del modulo:
  - `Attrezzature` nel form `Nuova / Modifica` non esisteva ancora nel JSX runtime;
  - `Calibra` esisteva nel viewer embedded del `Dettaglio`, ma il bottone usava una variante secondaria quasi invisibile su superficie chiara e risultava percepibile come comando assente.
- `src/next/NextManutenzioniPage.tsx` espone ora davvero `Mezzo`, `Compressore`, `Attrezzature` come 3 opzioni del tipo intervento.
- `src/next/domain/nextManutenzioniDomain.ts` accetta in modo retrocompatibile il nuovo tipo `attrezzature` senza rompere record legacy o salvataggi esistenti.
- `src/next/NextMappaStoricoPage.tsx` e `src/next/next-mappa-storico.css` rendono il comando `Calibra` chiaramente visibile nel toolbar tecnico, mantenendo invariata la grammatica viewer gia chiusa.
- Verifiche tecniche:
  - `npx eslint src/next/NextManutenzioniPage.tsx src/next/NextMappaStoricoPage.tsx src/next/domain/nextManutenzioniDomain.ts src/next/domain/nextMappaStoricoDomain.ts src/next/domain/nextManutenzioniGommeDomain.ts src/next/mezziHotspotAreas.ts` -> `OK`
  - `npm run build` -> `OK`
- Stato modulo:
  - `Manutenzioni` -> `PARZIALE`
