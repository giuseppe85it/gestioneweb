# Continuity Report - Home NEXT con Alert unico filtrabile per categorie reali

## Stato consegnato
- La card `Alert` della Home NEXT e unica e filtrabile.
- Il filtro di default e `Tutti`.
- Le categorie esposte sono quelle gia presenti nel runtime corrente.

## Punto fermo verificato
- Nessuna modifica a `src/pages/*`.
- Nessuna modifica alla logica di scrittura revisioni.
- Nessun nuovo writer o nuova shape dati.

## Prossimo passo corretto
- Se servono rifiniture ulteriori, restare nel solo perimetro `src/next/*` e aggiornare di nuovo i documenti di tracciamento.
